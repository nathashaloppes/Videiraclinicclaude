<% content_for :title, "Novo serviço" %>

<%= render "shared/back_button", path: admin_services_path %>

<h1 class="text-2xl font-semibold mb-6" style="color: #5D4037">Novo serviço</h1>

<div class="bg-white rounded-3xl shadow-lg p-6">
  <%= render "form", service: @service %>
</div>
