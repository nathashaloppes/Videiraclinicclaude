<% content_for :title, "Editar clínica" %>

<%= render "shared/back_button", path: admin_clinic_path(@clinic) %>

<h1 class="text-2xl font-semibold mb-6" style="color: #5D4037">Editar clínica</h1>

<div class="bg-white rounded-3xl shadow-lg p-6">
  <%= render "form", clinic: @clinic %>
</div>
