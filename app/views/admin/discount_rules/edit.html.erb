<% content_for :title, "Editar regra de desconto" %>
<div class="max-w-md">
  <h1 class="text-xl font-bold text-gray-900 mb-6">Editar regra de desconto</h1>
  <%= render "form", rule: @rule %>
</div>
