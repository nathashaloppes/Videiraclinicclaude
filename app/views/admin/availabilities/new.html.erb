<% content_for :title, "Novo Horário" %>

<%= render "shared/back_button", path: admin_availabilities_path %>

<h1 class="text-xl font-semibold mb-6" style="color: #5D4037">Novo horário</h1>

<div class="bg-white rounded-2xl shadow-sm p-5">
  <%= render "form", availability: @availability %>
</div>
